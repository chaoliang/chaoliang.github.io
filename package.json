{
  "name": "datetally",
  "version": "1.0.0",
  "description": "DateTally — fast, accurate date calculators. Static site, zero dependencies.",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "node build.js",
    "test": "node --test test/",
    "serve": "python3 -m http.server 4173 --directory dist"
  },
  "engines": {
    "node": ">=18"
  }
}
